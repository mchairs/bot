'use strict';

const assert = require('assert');
const optIn = require('./optin.resolution.js');
const sinon = require('sinon');

describe('OptIn', () => {
    it('It should call bot.reply', () => {
    });
});
