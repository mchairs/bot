'use strict';

if (!process.env.clientId || !process.env.clientSecret || !process.env.port) {
  console.log('Sorry dude, you need to specify a clientId, clientSecret and port before you can rock your bot');
  process.exit(1);
}

function test() {
}
